{
  "name": "hayaoshi-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Cloudflare Worker + Durable Object backend for the Hayaoshi (早押し) multiplayer kanji speed quiz.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.6.4",
    "@cloudflare/workers-types": "4.20241127.0",
    "typescript": "6.0.3",
    "vitest": "2.1.9",
    "wrangler": "3.90.0"
  }
}
