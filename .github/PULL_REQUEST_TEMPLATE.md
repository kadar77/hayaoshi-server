<!-- Thanks for contributing! -->

## What & why
Briefly describe the change and the motivation.

## How tested
- [ ] `npm run typecheck` passes
- [ ] `npm test` passes (scoring unit + full-flow integration)

## Notes
- If you changed the wire protocol (`src/protocol.ts`), did you update the
  matching types in the [kanji](https://github.com/kadar77/kanji) SPA and the
  validation in `src/validate.ts`?
